import { Container, Typography } from "@material-ui/core";

export const Navbar = () => {
  return (
    <Container>
      <Typography>Navbar</Typography>
    </Container>
  );
};

export default Navbar;
